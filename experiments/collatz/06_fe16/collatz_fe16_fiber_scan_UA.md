# ЗВІТ: СКАНУВАННЯ ВОЛОКНА НАЙГЛИБШОГО СТАНУ (40479, 0)

**Дата:** 2026-07-22  
**Етап:** Сканування волокна для найглибшого стану  
**Статус:** ✅ Завершено

---

## 1. РЕЗУЛЬТАТИ ВИКОНАННЯ

| Параметр | Значення |
| :--- | :--- |
| Досліджуваний стан | (40479, 0) |
| Ранг | 220 (максимальний) |
| Кількість протестованих представників | 10 000 |
| Діапазон q | 0 … 9 999 |
| Представники | n = 40479 + q · 65536 |
| Частка представників, що точно відповідають графу | **100%** |
| Середній час входження в граф | **0** |
| Максимальний час входження | **0** |

**Висновок:** Для всіх 10 000 протестованих представників волокна стану (40479, 0) виконується

Π₁₆(T(n)) = T₁₆(Π₁₆(n)),

тобто **проекція комутує з T точно з першого кроку**. Жодних затримок (помилок проекції) не виявлено.

---

## 2. ІНТЕРПРЕТАЦІЯ

### 2.1. Чому це важливо?

Найглибший стан (40479, 0) має найдовший шлях до термінального циклу — 220 кроків. Якщо для всього його волокна проекція є точною, це означає, що **будь-яке число з цього волокна** досягає 1 рівно за 220 кроків (плюс можливі коливання, але без додаткових затримок). Це сильний аргумент на користь того, що структура графа є «глобально узгодженою» принаймні для цього класу.

### 2.2. Чому час = 0?

У коді `steps_to_graph` визначався як мінімальне m ≥ 0 таке, що

Π₁₆(Tᵐ(n)) = T₁₆ᵐ(Π₁₆(n)).

Для всіх протестованих n це виконується вже при m = 0, тобто на самому початку траєкторії. Це означає, що для цих чисел **проекція і графовий перехід збігаються без будь-якого «розсинхрону»**.

### 2.3. Порівняння з попереднім тестом

Раніше, у випадковому тесті на 2000 станах, ми отримали максимальний час втечі τ = 183. Це означало, що деякі числа відхилялися від графа на 183 кроки. Але ці числа, ймовірно, належали до **інших волокон** — не до (40479, 0). Отже, помилки проекції не є універсальними; вони залежать від конкретного волокна.

---

## 3. НАСТУПНИЙ КРОК: ЗНАЙТИ ВОЛОКНА З ПОМИЛКАМИ ПРОЕКЦІЇ

Тепер, коли ми знаємо, що волокно найглибшого стану є «чистим», ми маємо знайти волокна, які **не** є чистими — тобто ті, де для деяких представників Π₁₆(T(n)) ≠ T₁₆(Π₁₆(n)). Саме ці волокна породжують затримки (τ > 1) у реальних траєкторіях.

### 3.1. Як знайти такі волокна?

Для кожного стану s ∈ S₁₆ визначимо множину помилок проекції:

E(s) = { n ∈ F₁₆(s) | Π₁₆(T(n)) ≠ T₁₆(s) }.

Якщо E(s) ≠ ∅, то волокно s є «проблемним». Ми можемо знайти такі стани, просканувавши невелику кількість представників для кожного стану (наприклад, 5–10) і перевіривши, чи виконується рівність Π₁₆(T(n)) = T₁₆(Π₁₆(n)).

### 3.2. Код для пошуку проблемних волокон

```python
"""
collatz_fe16_find_errors.py

Find states where projection error occurs.
For each state, test a few representatives and check if Π16(T(n)) == T16(Π16(n)).
"""

import os
import csv
from tqdm import tqdm

K = 16
M = 1 << K
NUM_SAMPLES = 5  # перевіряємо 5 представників на стан

# Функції
def v2(n):
    if n <= 0:
        return 0
    return (n & -n).bit_length() - 1

def v3(n):
    return min(v2(n), 3)

def pi(n):
    return (n % M, v3(n))

def T(n):
    if n % 2 == 0:
        return n // 2
    else:
        return 3 * n + 1

# Завантажуємо атлас
script_dir = os.path.dirname(os.path.abspath(__file__))
atlas_path = os.path.join(script_dir, "fe16_atlas_full.csv")

transitions = {}
states = []
with open(atlas_path, "r") as f:
    reader = csv.DictReader(f)
    for row in reader:
        r = int(row['r'])
        v = int(row['v'])
        nr = int(row['next_r'])
        nv = int(row['next_v'])
        transitions[(r, v)] = (nr, nv)
        states.append((r, v))

# Шукаємо проблемні стани
error_states = []
print(f"Testing {len(states)} states with {NUM_SAMPLES} samples each...")

for s in tqdm(states, desc="Scanning states"):
    r, v = s
    error_found = False
    # Шукаємо представників
    for q in range(NUM_SAMPLES * 10):
        n = r + q * M
        if n <= 0:
            continue
        if v3(n) != v:
            continue
        # Перевіряємо проекцію
        actual_next = pi(T(n))
        expected_next = transitions.get(s, None)
        if expected_next is None:
            continue
        if actual_next != expected_next:
            error_found = True
            break
        # Якщо знайшли помилку, виходимо з циклу
        if error_found:
            break
    if error_found:
        error_states.append(s)

print(f"\nFound {len(error_states)} states with projection errors.")

# Зберігаємо список
output_path = os.path.join(script_dir, "fe16_error_states.csv")
with open(output_path, "w") as f:
    writer = csv.writer(f)
    writer.writerow(["r", "v"])
    for r, v in error_states:
        writer.writerow([r, v])

print(f"Error states saved to {output_path}")
```

---

## 4. ОЧІКУВАНІ РЕЗУЛЬТАТИ

- Кількість проблемних станів має бути **невеликою** (можливо, кілька сотень або тисяч), оскільки більшість чисел поводяться «регулярно».
- Ці стани, ймовірно, відповідають числам з високою валюацією (v = 3), де обрізання валюації до 3 призводить до втрати інформації.
- Сканування цих волокон дозволить визначити, чи є для них скінченний час входження в граф.

---

## 5. ВИСНОВОК

Волокно найглибшого стану (40479, 0) є **ідеально чистим** — всі його представники точно відповідають графу. Це підтверджує, що графова структура є узгодженою, а єдина складність полягає в окремих волокнах, де проекція дає збій.

Тепер ми маємо знайти ці «проблемні» волокна і дослідити їхню структуру.

**Наступний звіт:** після виконання скрипта `collatz_fe16_find_errors.py`.